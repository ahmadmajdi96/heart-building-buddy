services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    ports:
      - "5555:5555"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 5555
    restart: unless-stopped
